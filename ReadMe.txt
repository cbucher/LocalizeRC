This project requires the Boost regex library, which must be installed on your development system to build this project properly.

Built versions of the app are in the UNICODE_Debug and UINICOE_Release if you'd like to test it before bothering to compile/build a new version.